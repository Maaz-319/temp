> GENERATED BY CHATGPT


Below are **5 LARGE, REAL PROGRAMMING TASKS** (not theory questions).
Each task is **designed so that you MUST implement code** and, together, they **cover every topic you listed**.

These are **exam + interview + real-engineering level** programs.
No spoon-feeding, no solutions — just **clear specs** like real assignments.

---

# 🔴 PROGRAM 1 — SMART BANKING SYSTEM (FOUNDATION + OPERATORS)

### 🎯 Goal

Build a **Bank Account System** with operator overloading, constructors, static members, templates, and abstraction.

### 📌 Requirements

1. Create an **abstract base class** `Account`

   * private balance
   * account number
   * pure virtual `calculateInterest()`
   * virtual destructor

2. Derived classes:

   * `SavingsAccount`
   * `CurrentAccount`

3. Implement:

   * default, parameterized, copy constructors
   * destructor (print message to track destruction)
   * getters/setters using `this`
   * constructor initialization lists

4. Static:

   * static `bankCode`
   * static function `getBankCode()`

5. Operator overloading:

   * `=` (deep copy)
   * `+` to add balances (global operator)
   * `<<` and `>>` for input/output
   * `==` to compare accounts

6. Templates:

   * Template function `printAny<T>()`
   * Template class `Logger<T>`

7. Demonstrate:

   * object slicing
   * base pointer → derived object
   * compile-time vs run-time binding

---

# 🔴 PROGRAM 2 — HOTEL MANAGEMENT SYSTEM (ADVANCED OOP + INHERITANCE)

### 🎯 Goal

Design a **Hotel Booking System** with rooms, halls, adapter class, and polymorphism.

### 📌 Requirements

1. Abstract base class `HotelService`

   * virtual `calculateBill()`

2. Room hierarchy:

   * `StandardRoom`, `DeluxeRoom`, `Suite`

3. Hall hierarchy:

   * `ConferenceHall`, `WeddingHall`

4. Use:

   * public, protected inheritance
   * initialization list in inheritance
   * override virtual functions
   * scope resolution to call parent method

5. Adapter class:

   * `LegacyBillingSystem`
   * Adapter converts it to `HotelService`

6. Demonstrate:

   * runtime polymorphism
   * object slicing bug and fix
   * dynamic_cast usage

---

# 🔴 PROGRAM 3 — CUSTOM CONTAINER LIBRARY (POINTERS + MEMORY)

### 🎯 Goal

Build your own **Dynamic Jagged Matrix + Linked List** library.

### 📌 Requirements

1. Create a **JaggedArray** class:

   * dynamic allocation using `int**`
   * sentinel values (`-1`) to mark row end
   * destructor to avoid leaks
   * function to detect dangling pointer risk

2. Implement:

   * pointer to pointer logic
   * copy constructor (deep copy)
   * assignment operator

3. Singly Linked List:

   * `Node` and `LinkedList` classes
   * aggregation (LinkedList uses Node)
   * insertion, deletion, traversal
   * destructor cleans all nodes

4. Use:

   * vectors of pairs
   * range-based loops with `auto` and `auto&`

---

# 🔴 PROGRAM 4 — UNIVERSITY SYSTEM (MULTIPLE INHERITANCE HELL 😈)

### 🎯 Goal

Design a **University Role System** demonstrating multiple inheritance, diamond problem, and virtual inheritance.

### 📌 Requirements

1. Base class `Person`

   * name, id
   * virtual function `getRole()`

2. Derived:

   * `Student`
   * `Employee`

3. Further derived:

   * `TeachingAssistant` inherits from both

4. Demonstrate:

   * diamond problem
   * fix using `virtual inheritance`
   * constructor order
   * ambiguity resolution using `::`

5. Show:

   * base pointer → derived object
   * illegal derived pointer → base object
   * dynamic_cast success and failure

---

# 🔴 PROGRAM 5 — FILE SYSTEM SIMULATOR (FINAL BOSS 🧠🔥)

### 🎯 Goal

Create a **File System Simulator** mixing abstraction, templates, adapters, and polymorphism.

### 📌 Requirements

1. Abstract base class `FileSystemEntity`

   * pure virtual `getSize()`
   * virtual destructor

2. Derived:

   * `File`
   * `Directory`

3. Directory:

   * stores vector of pointers to `FileSystemEntity`
   * aggregation (not ownership of external files)

4. Adapter class:

   * adapt `LegacyFileReader` to `FileSystemEntity`

5. Operator overloading:

   * `<<` for printing tree
   * `[]` to access child

6. Templates:

   * generic search function

7. Demonstrate:

   * runtime polymorphism
   * object slicing mistake and fix
   * constructor/destructor call order

---

# 📊 TOPIC COVERAGE CHECKLIST (ALL COVERED ✅)

| Topic                      | Program    |
| -------------------------- | ---------- |
| Constructors / destructors | 1, 3, 4, 5 |
| this pointer               | 1          |
| static members             | 1          |
| Jagged arrays + sentinel   | 3          |
| Pointer to pointer         | 3          |
| Operator overloading       | 1, 5       |
| Stream operators           | 1          |
| Friend functions           | 1          |
| Templates                  | 1, 5       |
| Aggregation                | 3, 5       |
| Initialization list        | ALL        |
| Vectors + auto             | 3, 5       |
| Linked list                | 3          |
| Inheritance                | ALL        |
| Virtual / polymorphism     | ALL        |
| Abstract classes           | ALL        |
| Scope resolution           | 2, 4       |
| Object slicing             | 1, 2, 5    |
| Base/derived pointers      | ALL        |
| Compile vs runtime binding | 1          |
| Multiple inheritance       | 4          |
| Diamond problem            | 4          |
| Virtual inheritance        | 4          |
| Adapter class              | 2, 5       |

---

# 🧠 HOW TO PRACTICE (IMPORTANT)

For **maximum learning**:

1. Write code **without internet**
2. Predict constructor/destructor order
3. Draw memory layout
4. Intentionally break code (see errors)
5. Fix using correct OOP design

---